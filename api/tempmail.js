const { TempMail } = require('1secmail-api');

exports.config = {
	name: "tempmail",
	author: 'Zishin Sama',
    description: 'Get temporary email (auto get inbox)',
    category: 'tools',
    usage: ['/tempmail']
}